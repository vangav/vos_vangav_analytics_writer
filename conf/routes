# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
# Commented out route generated by play
# GET     /                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)


# Generated routes
GET   /record_action                    com.vangav.vos_vangav_analytics_writer.controllers.record_action.ControllerRecordAction.getRecordAction()
GET   /reload_properties                com.vangav.vos_vangav_analytics_writer.controllers.reload_properties.ControllerReloadProperties.getReloadProperties()
